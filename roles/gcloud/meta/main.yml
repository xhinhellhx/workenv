---
galaxy_info:
  role_name: gcloud
  author: xhinhellhx
  description: Installs the Google Cloud CLI (gcloud)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_system == "Darwin"
