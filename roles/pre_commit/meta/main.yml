---
galaxy_info:
  role_name: pre_commit
  author: xhinhellhx
  description: Installs pre-commit (git hook framework)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_facts['system'] == "Darwin"
