---
# Install Neovim on macOS via Homebrew.
# Homebrew itself is ensured by the `homebrew` role dependency.

- name: Install Neovim
  community.general.homebrew:
    name: neovim
    state: present
    path: "{{ homebrew_prefix }}/bin"
