---
galaxy_info:
  role_name: rg
  author: xhinhellhx
  description: Installs ripgrep (rg), a fast recursive search tool
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_facts['system'] == "Darwin"
