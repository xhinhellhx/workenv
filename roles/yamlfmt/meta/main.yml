---
galaxy_info:
  role_name: yamlfmt
  author: xhinhellhx
  description: Installs yamlfmt (an extensible YAML formatter)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_facts['system'] == "Darwin"
