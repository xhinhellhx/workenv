---
galaxy_info:
  role_name: claude_code
  author: xhinhellhx
  description: Installs Claude Code via the official install script
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_facts['system'] == "Darwin"
