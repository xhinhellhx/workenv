---
# Install htop. macOS via Homebrew, Linux via the package manager.

- name: Install htop (macOS, Homebrew)
  community.general.homebrew:
    name: htop
    state: present
    path: "{{ homebrew_prefix }}/bin"
  when: ansible_facts['system'] == "Darwin"

- name: Install htop (Debian/Ubuntu)
  ansible.builtin.apt:
    name: htop
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
  when: ansible_facts['os_family'] == "Debian"

- name: Install htop (RHEL family)
  ansible.builtin.dnf:
    name: htop
    state: present
  become: true
  when: ansible_facts['os_family'] == "RedHat"
