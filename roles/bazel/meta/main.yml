---
galaxy_info:
  role_name: bazel
  author: xhinhellhx
  description: Installs the Bazel build system
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_facts['system'] == "Darwin"
