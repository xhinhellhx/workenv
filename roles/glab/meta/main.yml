---
galaxy_info:
  role_name: glab
  author: xhinhellhx
  description: Installs glab (the GitLab CLI)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_system == "Darwin"
