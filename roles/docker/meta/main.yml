---
galaxy_info:
  role_name: docker
  author: xhinhellhx
  description: Installs Docker (CLI + Colima runtime on macOS, Engine on Linux)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
    - name: Debian
    - name: Ubuntu
    - name: EL
dependencies:
  - role: homebrew
    when: ansible_facts['system'] == "Darwin"
