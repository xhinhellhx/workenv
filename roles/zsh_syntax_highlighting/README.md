# zsh_syntax_highlighting role

Installs [zsh-syntax-highlighting](https://github.com/zsh-users/zsh-syntax-highlighting)
into the oh-my-zsh custom plugins dir and enables it via
`plugins.d/90-zsh-syntax-highlighting.zsh` (numbered late, as the plugin must be
loaded near the end). Depends on `zsh_config`.
